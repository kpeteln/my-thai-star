# My Thai Star — Cloud Architecture Diagrams

> **Generated by**: architecture-analyzer  
> **Source**: Live analysis of `docker-compose.yml`, `docker-compose.traefik.yml`, `k8s.yml`, `k8s/aws/`, `k8s/azure/`, `reverse-proxy/nginx.conf`, `java/mtsj/architecture.json`, `node/ormconfig.json`, `net/docker-compose.yml`  
> **Application**: My Thai Star v3.2.0-SNAPSHOT — Multi-stack cloud-native restaurant management reference application

---

## 1. System Context Diagram (C4 Level 1)

Shows My Thai Star as a complete system in the context of its users and external services.

```mermaid
C4Context
    title System Context — My Thai Star Restaurant Application

    Person(customer, "Customer", "Browses menu, books tables,<br/>places orders online or via PWA")
    Person(waiter, "Waiter", "Manages orders and table<br/>assignments via cockpit UI")
    Person(manager, "Manager", "Administers menu, views reports,<br/>manages dish catalogue")

    System(mts, "My Thai Star", "Multi-stack restaurant management platform.<br/>Angular PWA frontend served by NGINX.<br/>Backend: Java Spring Boot (primary) ·<br/>Node.js NestJS · ASP.NET Core.<br/>Features: Table reservations · Orders ·<br/>Menu · ML Predictions · Clustering")

    System_Ext(smtp, "SMTP Mail Server", "Sends booking confirmations,<br/>guest invitations and order<br/>notifications (Gmail / Corporate SMTP)")
    System_Ext(ghcr, "GitHub Container Registry", "Hosts production Docker images:<br/>ghcr.io/devonfw/my-thai-star-angular<br/>ghcr.io/devonfw/my-thai-star-java")
    System_Ext(browser, "Web Browser / PWA", "Progressive Web App entry point,<br/>installable on mobile — offline-capable<br/>via service worker (ngsw-config.json)")
    System_Ext(electron, "Electron Desktop App", "Native desktop wrapper<br/>Windows (portable) · macOS (dmg)<br/>Linux (AppImage)")

    Rel(customer, browser, "Books tables, places orders", "HTTPS")
    Rel(waiter, browser, "Manages cockpit view", "HTTPS")
    Rel(manager, browser, "Administers menu and reports", "HTTPS")
    Rel(customer, electron, "Uses native desktop client", "Local IPC")

    Rel(browser, mts, "Single-Page App + REST API", "HTTP/HTTPS")
    Rel(electron, mts, "REST API calls", "HTTP/HTTPS")

    Rel(mts, smtp, "Sends transactional emails", "SMTP :25 / :587")
    Rel(ghcr, mts, "Supplies container images at deploy", "HTTPS pull")

    UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

---

## 2. Container Diagram (C4 Level 2)

Shows all deployable containers across all three backend stacks with their technology and interactions.

```mermaid
C4Container
    title Container Diagram — My Thai Star (All Stacks)

    Person(users, "Users", "Customer · Waiter · Manager")

    System_Boundary(mts, "My Thai Star Platform") {

        Container(traefik, "Reverse Proxy", "Traefik v2.0 OR NGINX",
            "Single HTTP entry point on :8081.<br/>Routes /* → Angular, /api/* → Backend.<br/>Traefik mode: Docker label-driven routing,<br/>health checks every 10s, dashboard :8080.<br/>NGINX mode: static proxy_pass rules.")

        Container(angular, "Angular SPA", "Angular 12+ · NGINX · PWA",
            "SPA served by NGINX (port 80).<br/>NgRx state management.<br/>Angular Material + Transloco i18n.<br/>PWA service worker (ngsw-config.json).<br/>Electron desktop wrapper.<br/>Endpoints: /health → 200 OK,<br/>/config → runtime JSON config.")

        Container(java_be, "Java Spring Boot API", "Java 11 · Spring Boot 2.4<br/>Devon4j · JAX-RS · JWT",
            "Primary backend. Port 8081.<br/>Context path: /mythaistar.<br/>9 domain modules: booking · order · dish<br/>user · image · mail · cluster · prediction<br/>· general. Flyway migrations.<br/>H2 in-memory DB (default profile).")

        Container(node_be, "Node.js NestJS API", "Node.js · NestJS · TypeORM<br/>Passport JWT · SQLite",
            "Alternative backend. Port 8081.<br/>Use-case pattern. Modules:<br/>booking · order · dish · image.<br/>SQLite in-memory DB. MailDev integration.<br/>Swagger/OpenAPI docs included.")

        Container(dotnet_be, ".NET Core API", "ASP.NET Core · EF Core<br/>JWT · Clean Architecture",
            "Alternative backend. Port 8081.<br/>Layers: WebApi · Business · Domain<br/>· Infrastructure. JWT auth.<br/>MSSQL Server database.<br/>Custom SMTP mail microservice.")

        ContainerDb(h2_db, "H2 In-Memory DB", "H2 · Flyway",
            "Embedded Java database.<br/>Active profile: h2mem.<br/>Tables: Booking · Order · OrderLine<br/>· Dish · Category · Ingredient<br/>· Image · User · InvitedGuest.")

        ContainerDb(sqlite_db, "SQLite In-Memory DB", "SQLite · TypeORM",
            "Embedded Node.js database.<br/>Auto-migrated on startup.<br/>Mirrors Java schema.")

        ContainerDb(mssql_db, "MS SQL Server", "SQL Server 2017 Linux",
            "Relational DB for .NET backend.<br/>Port 1433. EF Core migrations.<br/>Static IP: 172.20.128.5.")

        Container(maildev, "MailDev / SMTP Service", "MailDev · Custom .NET SMTP",
            "Dev mail catcher (Node stack): MailDev<br/>SMTP :25 · Web UI :80.<br/>Custom SMTP microservice (.NET stack)<br/>on port :2025.")
    }

    System_Ext(smtp_ext, "External SMTP", "Gmail SMTP / Corporate SMTP Server")
    System_Ext(ghcr_ext, "GitHub Container Registry", "ghcr.io/devonfw/*")

    Rel(users, traefik, "HTTP/HTTPS requests", ":8081 (Docker) / :443 (K8s)")
    Rel(traefik, angular, "/* → SPA", "HTTP :80")
    Rel(traefik, java_be, "/api/* → /mythaistar/*", "HTTP :8081")
    Rel(traefik, node_be, "/api/* → /mythaistar/*", "HTTP :8081 (Node stack)")
    Rel(traefik, dotnet_be, "/api/* → backend", "HTTP :8081 (.NET stack)")

    Rel(java_be, h2_db, "Spring Data JPA / Hibernate", "JDBC in-process")
    Rel(node_be, sqlite_db, "TypeORM repositories", "In-process")
    Rel(dotnet_be, mssql_db, "Entity Framework Core", "TCP :1433")

    Rel(java_be, smtp_ext, "Spring Mail / JavaMail", "SMTP :25 / :587")
    Rel(node_be, maildev, "Nodemailer", "SMTP :25")
    Rel(dotnet_be, maildev, "Custom SMTP microservice", "SMTP :2025")

    Rel(ghcr_ext, angular, "docker pull at deploy", "HTTPS")
    Rel(ghcr_ext, java_be, "docker pull at deploy", "HTTPS")

    UpdateLayoutConfig($c4ShapeInRow="3", $c4BoundaryInRow="1")
```

---

## 3. Docker Compose — Local Development Deployments

### 3a. Primary Java Stack (NGINX Reverse Proxy)

Derived from `docker-compose.yml` and `reverse-proxy/nginx.conf`.

```mermaid
graph TB
    subgraph HOST["🖥️  Developer Host Machine"]
        PORT["localhost:8081"]
    end

    subgraph NET["🐳 Docker Bridge Network: my-thai-star"]
        direction TB

        subgraph RP["mts_reverse_proxy · NGINX"]
            direction LR
            RP_IMG["Image: my-thai-star/reverse-proxy:latest<br/>Built: reverse-proxy/Dockerfile<br/>Base: nginx:latest + custom nginx.conf<br/>Internal port :80 → Host :8081"]
            RP_RULES["Routing rules (nginx.conf):<br/>━━━━━━━━━━━━━━━━━━━━━━━<br/>location /    → proxy_pass http://angular:80<br/>location /api → proxy_pass http://java:8081/mythaistar<br/>Sets X-Forwarded-* headers<br/>WebSocket upgrade support"]
        end

        subgraph ANG["mts_angular · Angular + NGINX"]
            ANG_IMG["Image: my-thai-star/angular:latest<br/>Built: angular/Dockerfile<br/>Serves SPA from /usr/share/nginx/html<br/>GET /health → 200 OK (probe)<br/>GET /config → docker-external-config.json"]
        end

        subgraph JAVA["mts_java · Spring Boot"]
            JAVA_IMG["Image: my-thai-star/java:latest<br/>Built: java/Dockerfile (multi-stage Maven + JRE 11)<br/>WAR: mythaistar-bootified.war<br/>Context path: /mythaistar · Port: 8081<br/>DB: H2 in-memory (spring.profiles.active=h2mem)<br/>Flyway: classpath:db/migration + db/specific/h2<br/>Healthcheck: /mythaistar/services/rest/dishmanagement/v1/category/0/"]
        end

        H2[("H2 In-Memory DB\n(embedded in JVM process)")]
    end

    PORT -->|"HTTP :80 (mapped to host :8081)"| RP
    RP -->|"proxy_pass /* to :80"| ANG
    RP -->|"proxy_pass /api to :8081/mythaistar"| JAVA
    JAVA --- H2

    style HOST fill:#f5f5f5,stroke:#9e9e9e
    style NET fill:#e3f2fd,stroke:#1565c0,color:#000
    style RP fill:#fff3e0,stroke:#e65100,color:#000
    style ANG fill:#e8f5e9,stroke:#2e7d32,color:#000
    style JAVA fill:#fce4ec,stroke:#880e4f,color:#000
    style H2 fill:#ede7f6,stroke:#4527a0,color:#000
```

### 3b. Traefik Stack (Dynamic Service Discovery)

Derived from `docker-compose.traefik.yml` and `reverse-proxy/traefik/docker-compose.yml`.

```mermaid
graph TB
    subgraph HOST["🖥️  Developer Host Machine"]
        P80["localhost:8081\n(HTTP traffic)"]
        P8080["localhost:8080\n(Traefik Dashboard)"]
    end

    subgraph NET["🐳 Docker Bridge Network: my-thai-star"]
        direction TB

        subgraph TRK["Traefik v2.0 · Reverse Proxy + Load Balancer"]
            TRK_CFG["Image: traefik:2.0<br/>Providers: --providers.docker=true<br/>Entrypoint: --entrypoints.web.address=:80<br/>Dashboard: --api.dashboard=true (insecure)<br/>Reads Docker socket for live config updates<br/>Volume: /var/run/docker.sock → /var/run/docker.sock"]
            TRK_MW["Middleware chain 'fixpath':<br/>Step 1: remove-api → StripPrefix /api<br/>Step 2: add-mythaistar → AddPrefix /mythaistar<br/>Result: /api/foo → /mythaistar/foo"]
        end

        subgraph ANG["Angular Container · NGINX"]
            ANG_LBL["Router: PathPrefix('/')<br/>Priority: lower (catch-all)<br/>Health: GET /health every 10s<br/>Image: my-thai-star/angular:latest"]
        end

        subgraph JAVA["Java Spring Boot Container"]
            JAVA_LBL["Router: PathPrefix('/api/')<br/>Priority: higher (more specific)<br/>Middleware: fixpath chain applied<br/>Health: GET /mythaistar/services/rest/<br/>dishmanagement/v1/category/0/ every 10s<br/>Image: my-thai-star/java:latest"]
        end

        SOCK[("🔌 Docker Socket\n/var/run/docker.sock")]
    end

    P80 -->|"HTTP :80"| TRK
    P8080 -->|"Dashboard UI"| TRK
    TRK -->|"PathPrefix('/') → catch-all SPA"| ANG
    TRK -->|"PathPrefix('/api/')\n→ middleware fixpath\n→ /mythaistar/*"| JAVA
    TRK --- SOCK

    style HOST fill:#f5f5f5,stroke:#9e9e9e
    style NET fill:#e3f2fd,stroke:#1565c0
    style TRK fill:#fff8e1,stroke:#f57f17
    style ANG fill:#e8f5e9,stroke:#2e7d32
    style JAVA fill:#fce4ec,stroke:#880e4f
    style SOCK fill:#f3e5f5,stroke:#6a1b9a
```

### 3c. Node.js Stack

Derived from `node/docker-compose.yml`.

```mermaid
graph TB
    subgraph HOST["🖥️  Developer Host Machine"]
        P8091["localhost:8091\n(HTTP traffic)"]
        P8092["localhost:8092\n(MailDev Web UI)"]
        P1025["localhost:1025\n(SMTP capture)"]
    end

    subgraph NET["🐳 Docker Bridge Network: my-thai-star-node"]
        direction TB

        subgraph RP["mts_reverse_proxy_node · Reverse Proxy"]
            RP_CFG["Image: devonfwforge/reverse-proxy:latest<br/>FRONTEND_ENV=http://angular:80<br/>BACKEND_ENV=http://node:8081/mythaistar<br/>depends_on: [angular, node]<br/>Port: :80 → host :8091"]
        end

        subgraph ANG["mts_angular_node · Angular SPA"]
            ANG_INFO["Image: my-thai-star/angular:latest<br/>Built from: ../angular/<br/>Shared image with Java stack<br/>Internal port: 80"]
        end

        subgraph NODE["mts_node · NestJS API"]
            NODE_INFO["Image: my-thai-star/node:latest<br/>Built from: node/<br/>NODE_ENV=production<br/>TypeORM: SQLite in-memory (:memory:)<br/>migrationsRun: true on startup<br/>depends_on: [maildev]"]
        end

        subgraph MAIL["maildev · Email Catcher"]
            MAIL_INFO["Image: maildev/maildev<br/>SMTP listener: :25 → host :1025<br/>Web UI: :80 → host :8092<br/>Intercepts all outgoing emails"]
        end

        SQLITE[("SQLite In-Memory\n(embedded in Node process)")]
    end

    P8091 --> RP
    P8092 --> MAIL
    P1025 --> MAIL
    RP -->|"/* → angular:80"| ANG
    RP -->|"/api → node:8081/mythaistar"| NODE
    NODE -->|"Nodemailer SMTP :25"| MAIL
    NODE --- SQLITE

    style HOST fill:#f5f5f5,stroke:#9e9e9e
    style NET fill:#e3f2fd,stroke:#1565c0
    style RP fill:#fff3e0,stroke:#e65100
    style ANG fill:#e8f5e9,stroke:#2e7d32
    style NODE fill:#e0f7fa,stroke:#006064
    style MAIL fill:#f3e5f5,stroke:#6a1b9a
    style SQLITE fill:#ede7f6,stroke:#4527a0
```

### 3d. .NET Core Stack

Derived from `net/docker-compose.yml`.

```mermaid
graph TB
    subgraph HOST["🖥️  Developer Host Machine"]
        P4200["localhost:4200\n(Angular client)"]
        P8081[" localhost:8081\n(.NET Core API)"]
        P2025["localhost:2025\n(SMTP Mail)"]
        P1401["localhost:1401\n(MSSQL → :1433)"]
    end

    subgraph NET["🐳 Docker Static Network: 172.20.0.0/16"]
        direction TB

        subgraph CLIENT["client · Angular SPA (172.20.128.3)"]
            CLI_INFO["Built from: net/angular<br/>Port: :80 → host :4200<br/>depends_on: [server, mail]"]
        end

        subgraph SERVER[".NET Core API Server (172.20.128.4)"]
            SRV_INFO["Built from: net/netcore<br/>ASP.NET Core + Entity Framework Core<br/>JWT authentication · Clean Architecture<br/>Port: :8081 → host :8081<br/>depends_on: [mail]<br/>Projects: WebApi · Business · Domain<br/>· Repositories · UnitOfWork · JWT"]
        end

        subgraph MAIL["mail · SMTP Mail Service (172.20.128.2)"]
            MAIL_INFO["Custom .NET SMTP microservice<br/>Projects: Mail · AOP · Resources<br/>Port: :2025 → host :2025"]
        end

        subgraph DB["db · MS SQL Server (172.20.128.5)"]
            DB_INFO["Image: microsoft/mssql-server-linux<br/>SA_PASSWORD: set via env<br/>ACCEPT_EULA: Y<br/>Port: :1433 → host :1401"]
        end
    end

    P4200 --> CLIENT
    P8081 --> SERVER
    P2025 --> MAIL
    P1401 --> DB
    CLIENT -->|"REST API calls"| SERVER
    SERVER -->|"Entity Framework TCP :1433"| DB
    SERVER -->|"SMTP :2025"| MAIL

    style HOST fill:#f5f5f5,stroke:#9e9e9e
    style NET fill:#e3f2fd,stroke:#1565c0
    style CLIENT fill:#e8f5e9,stroke:#2e7d32
    style SERVER fill:#e8eaf6,stroke:#283593
    style MAIL fill:#f3e5f5,stroke:#6a1b9a
    style DB fill:#ede7f6,stroke:#4527a0
```

---

## 4. Kubernetes Production Deployments

### 4a. Base Kubernetes Manifest (Okteto / Generic Cluster)

Derived from `k8s.yml`. Deployed to Okteto cloud at `mts-devonfw-core.cloud.okteto.net`.

```mermaid
graph TB
    subgraph INTERNET["🌐 Internet"]
        USER["End Users / Browsers"]
    end

    subgraph K8S["☸️  Kubernetes Cluster — app label: mts"]
        direction TB

        subgraph INGRESS_LAYER["Ingress Layer (kubernetes.io/ingress.class: nginx)"]
            ING_ANG["Ingress: angular<br/>Host: mts-devonfw-core.cloud.okteto.net<br/>TLS: ✅ (host in spec.tls)<br/>Path: /* → Service angular :80<br/>PathType: Prefix"]
            ING_JAVA["Ingress: java<br/>Host: mts-devonfw-core.cloud.okteto.net<br/>TLS: ✅ (host in spec.tls)<br/>Path: /api(/)(.*)  → Service java :8081<br/>Annotation: nginx rewrite-target /mythaistar/$2<br/>Annotation: dev.okteto.com/generate-host: mts"]
        end

        subgraph SVC_LAYER["Service Layer"]
            SVC_ANG["Service: angular<br/>Type: ClusterIP<br/>Port: 80 → targetPort: http(80)<br/>Selector: app=mts, tier=angular"]
            SVC_JAVA["Service: java<br/>Type: ClusterIP<br/>Port: 8081 → targetPort: 8081<br/>Selector: app=mts, tier=java"]
        end

        subgraph DEPLOY_LAYER["Deployment Layer"]
            DEP_ANG["Deployment: angular<br/>Replicas: 1<br/>Labels: app=mts, tier=angular<br/>Image: ghcr.io/devonfw/my-thai-star-angular:latest<br/>imagePullPolicy: Always<br/>containerPort: 80"]
            DEP_JAVA["Deployment: java<br/>Replicas: 1<br/>Labels: app=mts, tier=java<br/>Image: ghcr.io/devonfw/my-thai-star-java:latest<br/>imagePullPolicy: Always<br/>containerPort: 8081"]
        end

        H2[("H2 In-Memory DB\nEmbedded in java Pod\nNo PersistentVolumeClaim")]
    end

    subgraph REGISTRY["📦 GitHub Container Registry"]
        GHCR["ghcr.io/devonfw/\nmy-thai-star-angular:latest\nmy-thai-star-java:latest"]
    end

    USER -->|"HTTPS"| ING_ANG
    USER -->|"HTTPS /api/*"| ING_JAVA
    ING_ANG --> SVC_ANG
    ING_JAVA --> SVC_JAVA
    SVC_ANG --> DEP_ANG
    SVC_JAVA --> DEP_JAVA
    DEP_JAVA --- H2
    GHCR -.->|"imagePullPolicy: Always\nat every pod start"| DEP_ANG
    GHCR -.->|"imagePullPolicy: Always\nat every pod start"| DEP_JAVA

    style K8S fill:#e3f2fd,stroke:#1565c0
    style INGRESS_LAYER fill:#fff3e0,stroke:#e65100
    style SVC_LAYER fill:#e8f5e9,stroke:#2e7d32
    style DEPLOY_LAYER fill:#fce4ec,stroke:#880e4f
    style H2 fill:#ede7f6,stroke:#4527a0
    style REGISTRY fill:#f1f8e9,stroke:#558b2f
    style INTERNET fill:#f5f5f5,stroke:#9e9e9e
```

### 4b. AWS EKS Deployment

Derived from `k8s/aws/` manifests. Region: `eu-west-1`. Host: `ab1c2d100…eu-west-1.elb.amazonaws.com`.

```mermaid
graph TB
    subgraph INTERNET["🌐 Internet"]
        USERS["End Users"]
    end

    subgraph AWS["☁️  Amazon Web Services — eu-west-1"]
        direction TB

        subgraph ELB_LAYER["AWS Elastic Load Balancer"]
            ELB["AWS Network / Classic ELB\nab1c2d100…eu-west-1.elb.amazonaws.com\nHTTPS :443 (TLS in spec.tls)"]
        end

        subgraph EKS["Amazon EKS Cluster — namespace: default"]
            direction TB

            subgraph INGRESS["NGINX Ingress Controller\n(kubernetes.io/ingress.class: nginx)"]
                ING_FE["Ingress: mts-frontend\nHost: *.eu-west-1.elb.amazonaws.com\nPath: /* → mts-frontend :80\nssl-redirect: false\nPathType: ImplementationSpecific"]
                ING_BE["Ingress: mts-backend\nHost: *.eu-west-1.elb.amazonaws.com\nPath: /api(/)(.*)→ mts-backend :8081\nRewrite-target: /mythaistar/$2\nssl-redirect: false\nPathType: ImplementationSpecific"]
            end

            subgraph SERVICES["Kubernetes Services"]
                SVC_FE["Service: mts-frontend\nType: ClusterIP\nPort: 80\nSelector: app=mts, tier=mts-frontend"]
                SVC_BE["Service: mts-backend\nType: ClusterIP\nPort: 8081\nSelector: app=mts, tier=mts-backend"]
            end

            subgraph PODS["Pod Deployments"]
                POD_FE["Deployment: mts-frontend\nReplicas: 1\nImage: ghcr.io/devonfw/my-thai-star-angular:latest\ncontainerPort: 80\nimagePullPolicy: Always"]
                POD_BE["Deployment: mts-backend\nReplicas: 1\nImage: ghcr.io/devonfw/my-thai-star-java:latest\ncontainerPort: 8081\nimagePullPolicy: Always"]
            end

            H2[("H2 In-Memory DB\nembedded in backend pod\n⚠️ Data lost on pod restart")]
        end
    end

    GHCR["📦 ghcr.io/devonfw/*"]

    USERS -->|"HTTPS"| ELB
    ELB --> ING_FE
    ELB --> ING_BE
    ING_FE --> SVC_FE
    ING_BE --> SVC_BE
    SVC_FE --> POD_FE
    SVC_BE --> POD_BE
    POD_BE --- H2
    GHCR -.->|"imagePullPolicy: Always"| POD_FE
    GHCR -.->|"imagePullPolicy: Always"| POD_BE

    style AWS fill:#fff8e1,stroke:#f57f17
    style EKS fill:#e3f2fd,stroke:#1565c0
    style INGRESS fill:#fff3e0,stroke:#e65100
    style SERVICES fill:#e8f5e9,stroke:#2e7d32
    style PODS fill:#fce4ec,stroke:#880e4f
    style H2 fill:#ede7f6,stroke:#4527a0
    style INTERNET fill:#f5f5f5,stroke:#9e9e9e
```

### 4c. Azure AKS Deployment

Derived from `k8s/azure/` manifests. Region: `westeurope`. Host: `100001xx…westeurope.aksapp.io`.

```mermaid
graph TB
    subgraph INTERNET["🌐 Internet"]
        USERS["End Users"]
    end

    subgraph AZURE["☁️  Microsoft Azure — West Europe"]
        direction TB

        subgraph AGW["Azure Application Gateway / HTTP Routing Add-on"]
            AGW_FE["100001xx…westeurope.aksapp.io\nHTTPS :443\nIngress class: addon-http-application-routing"]
        end

        subgraph AKS["Azure Kubernetes Service (AKS) — namespace: default"]
            direction TB

            subgraph INGRESS["HTTP Application Routing Ingress"]
                ING_FE["Ingress: mts-frontend\nHost: *.westeurope.aksapp.io\nPath: /* → mts-frontend :80\nssl-redirect: false"]
                ING_BE["Ingress: mts-backend\nHost: *.westeurope.aksapp.io\nPath: /api(/)(.*)→ mts-backend :8081\nRewrite: /mythaistar/$2\nssl-redirect: false"]
            end

            subgraph SERVICES["Kubernetes Services (ClusterIP)"]
                SVC_FE["Service: mts-frontend\nPort: 80"]
                SVC_BE["Service: mts-backend\nPort: 8081"]
            end

            subgraph PODS["Pod Deployments"]
                POD_FE["Deployment: mts-frontend\nReplicas: 1\nImage: ghcr.io/devonfw/my-thai-star-angular:latest\ncontainerPort: 80"]
                POD_BE["Deployment: mts-backend\nReplicas: 1\nImage: ghcr.io/devonfw/my-thai-star-java:latest\ncontainerPort: 8081"]
            end

            H2[("H2 In-Memory DB\nembedded in backend pod")]
        end
    end

    GHCR["📦 ghcr.io/devonfw/* (or ACR mirror)"]

    USERS -->|"HTTPS"| AGW
    AGW --> ING_FE
    AGW --> ING_BE
    ING_FE --> SVC_FE
    ING_BE --> SVC_BE
    SVC_FE --> POD_FE
    SVC_BE --> POD_BE
    POD_BE --- H2
    GHCR -.->|"imagePullPolicy: Always"| POD_FE
    GHCR -.->|"imagePullPolicy: Always"| POD_BE

    style AZURE fill:#e3f2fd,stroke:#0d47a1
    style AKS fill:#e8eaf6,stroke:#283593
    style INGRESS fill:#fff3e0,stroke:#e65100
    style SERVICES fill:#e8f5e9,stroke:#2e7d32
    style PODS fill:#fce4ec,stroke:#880e4f
    style H2 fill:#ede7f6,stroke:#4527a0
    style INTERNET fill:#f5f5f5,stroke:#9e9e9e
```

---

## 5. CI/CD Pipeline Architecture

Derived from `jenkins/deployment/Jenkinsfile`. Supplemented by GitHub Actions (`.github/`) and Travis CI (`.travis.yml`).

```mermaid
flowchart TD
    subgraph DEV["👩‍💻 Developer Workflow"]
        CODE["Code Commit\ngit push to GitHub"]
    end

    subgraph SCM["📁 GitHub Repository\nkpeteln/my-thai-star"]
        PR["Pull Request / Branch Push"]
        GHCR_REG["GitHub Container Registry\nghcr.io/devonfw/my-thai-star-angular:latest\nghcr.io/devonfw/my-thai-star-java:latest"]
    end

    subgraph CI["🔄 Continuous Integration"]
        TRAVIS["Travis CI\n(.travis.yml)\nBuild + Unit Tests"]
        GHA["GitHub Actions\nBuild → Push → ghcr.io"]
    end

    subgraph JENKINS["🔧 Jenkins CI/CD Server\nDeployment Pipeline"]
        direction TB
        J_PARAMS["Parameters:\n• registryUrl (Nexus private registry)\n• registryCredentialsId (nexus-api)\n• buildReverseProxy (boolean)\n• port (default: 8081)\n• dockerNetwork (default: my-thai-star)"]
        J1["Stage 1: Load Custom Tools\ntool 'docker-global'"]
        J2["Stage 2: Create Docker Network\ndocker network ls -f name=my-thai-star\nCreate if not exists (idempotent)"]
        J3{"buildReverseProxy\n== true?"}
        J4["Stage 3: Build Docker Image\ncd reverse-proxy/\ndocker build my-thai-star/reverse-proxy:latest\ndocker push → Nexus Registry"]
        J5["Stage 4: Deploy New Image\ndocker pull latest from Nexus\nRemove existing container (if any)\ndocker run -d -p {port}:80\n--network my-thai-star\n--network-alias reverse-proxy"]
        J6["Post (always): cleanWs()\nWorkspace cleanup"]
    end

    subgraph NEXUS["🗄️ Nexus Private Registry\n(REPLACE_ME URL)"]
        NX_IMGS["Stores built Docker images\nmy-thai-star/reverse-proxy:latest"]
    end

    subgraph TARGET["🖥️ Deployment Target Host"]
        RUNNING["Running Docker Containers:\nmts_reverse_proxy\nmts_angular\nmts_java\nNetwork: my-thai-star\nPort: 8081 exposed"]
    end

    CODE --> PR
    PR --> TRAVIS
    PR --> GHA
    PR --> JENKINS

    GHA -->|"docker push"| GHCR_REG
    GHCR_REG -.->|"K8s imagePullPolicy: Always"| TARGET

    J_PARAMS -.->|"configures stages"| J1
    J1 --> J2 --> J3
    J3 -->|"Yes"| J4 --> J5
    J3 -->|"No (skip rebuild)"| J5
    J4 -->|"push image"| NEXUS
    J5 -->|"pull image"| NEXUS
    J5 --> J6
    J5 -->|"container started"| RUNNING

    style DEV fill:#f5f5f5,stroke:#9e9e9e
    style SCM fill:#e8f5e9,stroke:#2e7d32
    style CI fill:#e0f7fa,stroke:#006064
    style JENKINS fill:#fff3e0,stroke:#e65100
    style NEXUS fill:#ede7f6,stroke:#4527a0
    style TARGET fill:#e3f2fd,stroke:#1565c0
```

---

## 6. Network Architecture & Request Routing

Complete request flow from browser through every routing layer to the backend.

```mermaid
flowchart LR
    subgraph CLIENT["🌐 Client"]
        BROWSER["Browser / PWA\nor Electron App"]
    end

    subgraph ENTRY["Entry Point"]
        direction TB
        EP_DOCKER["Docker Compose\nHost :8081"]
        EP_K8S["Kubernetes\nIngress Controller\nHTTPS :443"]
    end

    subgraph PROXY["Reverse Proxy Layer"]
        direction TB

        subgraph NGINX_MODE["NGINX Mode\n(docker-compose.yml)"]
            NGINX_RULE1["location /\nproxy_pass http://angular:80\n+ X-Forwarded-* headers\n+ WebSocket upgrade support"]
            NGINX_RULE2["location /api\nproxy_pass http://java:8081/mythaistar\n+ X-Forwarded-* headers"]
        end

        subgraph TRAEFIK_MODE["Traefik Mode\n(docker-compose.traefik.yml)"]
            TRK_RULE1["PathPrefix('/')\nRouter: angular\n→ angular container :80"]
            TRK_RULE2["PathPrefix('/api/')\nRouter: java + middleware fixpath\n→ strip /api → add /mythaistar\n→ java container :8081"]
        end

        subgraph K8S_MODE["Kubernetes Mode\n(k8s.yml / k8s/aws/ / k8s/azure/)"]
            K8S_RULE1["path: /*\nIngress: angular/mts-frontend\n→ Service angular :80"]
            K8S_RULE2["path: /api(/)(.*)  \nIngress: java/mts-backend\nrewrite-target: /mythaistar/\$2\n→ Service java :8081"]
        end
    end

    subgraph BACKENDS["Backend Services"]
        direction TB
        ANG_SRV["Angular NGINX Container\nPort :80\nServes /usr/share/nginx/html\ntry_files \$uri \$uri/ /index.html\n(HTML5 SPA routing fallback)\nRuntime config: /config endpoint\nHealth: /health → 200 OK"]
        JAVA_SRV["Java Spring Boot\nPort :8081\nContext: /mythaistar\nREST: /mythaistar/services/rest/**\nSwagger: /mythaistar/swagger-ui.html\nJWT secured endpoints\nH2 / PostgreSQL / SAP HANA"]
    end

    BROWSER -->|"HTTP(S)"| EP_DOCKER
    BROWSER -->|"HTTPS"| EP_K8S
    EP_DOCKER --> NGINX_MODE
    EP_DOCKER --> TRAEFIK_MODE
    EP_K8S --> K8S_MODE

    NGINX_RULE1 -->|"proxy_pass :80"| ANG_SRV
    NGINX_RULE2 -->|"proxy_pass :8081"| JAVA_SRV
    TRK_RULE1 -->|"forward :80"| ANG_SRV
    TRK_RULE2 -->|"forward :8081"| JAVA_SRV
    K8S_RULE1 -->|"ClusterIP :80"| ANG_SRV
    K8S_RULE2 -->|"ClusterIP :8081"| JAVA_SRV

    style CLIENT fill:#f5f5f5,stroke:#9e9e9e
    style ENTRY fill:#fff3e0,stroke:#e65100
    style PROXY fill:#e3f2fd,stroke:#1565c0
    style NGINX_MODE fill:#e8f5e9,stroke:#2e7d32
    style TRAEFIK_MODE fill:#fff8e1,stroke:#f57f17
    style K8S_MODE fill:#e8eaf6,stroke:#283593
    style BACKENDS fill:#fce4ec,stroke:#880e4f
```

---

## 7. Recommended Production Cloud Architecture (AWS)

A target-state architecture for My Thai Star on AWS, building upon the existing K8s infrastructure patterns.

```mermaid
graph TB
    subgraph INTERNET["🌐 Internet"]
        USERS["End Users\n(Browser · PWA · Electron)"]
    end

    subgraph AWS["☁️  Amazon Web Services — eu-west-1 (Recommended Production Target)"]
        direction TB

        subgraph EDGE["Edge / DNS Layer"]
            R53["🔵 Route 53\nDNS · Health routing\nmythaistar.example.com"]
            CF["🔵 CloudFront CDN\nSSL/TLS termination (ACM)\nCache Angular static assets\nCompress & edge-serve SPA"]
        end

        subgraph VPC["VPC — 10.0.0.0/16"]
            direction TB

            subgraph PUBLIC_SUBNETS["Public Subnets (AZ-a 10.0.1.0/24 · AZ-b 10.0.2.0/24)"]
                ALB["🔵 Application Load Balancer\nHTTPS :443 (ACM cert)\nHTTP → HTTPS redirect\nTarget groups: EKS node groups"]
            end

            subgraph EKS_CLUSTER["Amazon EKS Cluster (Private Subnets 10.0.10.0/24 · 10.0.11.0/24)"]
                direction TB

                subgraph INGRESS_NS["nginx-ingress namespace"]
                    ING_CTRL["NGINX Ingress Controller\nPath routing:\n  /* → mts-frontend Service\n  /api/* → mts-backend Service\nURL rewrite: /api/* → /mythaistar/*\nTLS termination at ALB"]
                end

                subgraph MTS_NS["mts namespace"]
                    FE_DEP["mts-frontend Deployment\nImage: ECR/my-thai-star-angular:latest\nReplicas: 2 (across AZs)\nResources: 128m CPU · 256Mi RAM"]
                    BE_DEP["mts-backend Deployment\nImage: ECR/my-thai-star-java:latest\nReplicas: 2 (across AZs)\nResources: 500m CPU · 1Gi RAM\nEnv vars from Secrets Manager"]
                    HPA_FE["HPA: mts-frontend\nCPU target: 70%\nMin: 1 · Max: 5 replicas"]
                    HPA_BE["HPA: mts-backend\nCPU target: 70%\nMin: 2 · Max: 10 replicas"]
                end
            end

            subgraph DATA["Data Layer (Private Subnets 10.0.20.0/24 · 10.0.21.0/24)"]
                RDS["🔵 Amazon RDS PostgreSQL\ndb.t3.medium · Multi-AZ\nAutomated backups 7 days\nEncryption at rest (KMS)\n⬆️ Replaces H2 in-memory"]
                REDIS["🔵 ElastiCache Redis\ncache.t3.micro · cluster mode off\nSession + API response caching\n⬆️ New addition"]
            end

            subgraph SECURITY["Security Services"]
                SM["🔵 Secrets Manager\nDB credentials · JWT secret\nSMTP credentials · API keys\nAuto-rotation enabled"]
                KMS["🔵 KMS\nEncryption keys for RDS\nand Secrets Manager"]
                IAM_ROLES["🔵 IAM Roles\nEKS Pod Identity\nLeast-privilege policies\nIRSA for service accounts"]
            end
        end

        subgraph REGISTRY["Container Registry"]
            ECR["🔵 Amazon ECR\nPrivate registry (eu-west-1)\nImage scanning on push\nLifecycle policies\nMirror of ghcr.io images"]
        end

        subgraph OBSERVABILITY["Observability Stack"]
            CW_LOGS["🔵 CloudWatch Logs\nApplication logs\nNGINX access logs\nEKS control plane logs"]
            CW_METRICS["🔵 CloudWatch Metrics\nCustom dashboards\nALB · EKS · RDS metrics"]
            CW_ALARMS["🔵 CloudWatch Alarms\n5xx error rate > 1%\nRDS CPU > 80%\nPod OOMKill alerts → SNS"]
            XRAY["🔵 AWS X-Ray\nDistributed tracing\nRequest path visualisation"]
        end

        subgraph EMAIL["Email Service"]
            SES["🔵 Amazon SES\nTransactional email\nBooking confirmations\nGuest invitations\nSPF · DKIM configured\n⬆️ Replaces Gmail SMTP"]
        end

        subgraph CICD_LAYER["CI/CD Pipeline"]
            GHA["⚙️ GitHub Actions\nBuild → Test → Push ECR\n→ kubectl rollout EKS\nEnvironments: staging · prod"]
        end
    end

    USERS -->|"HTTPS"| R53
    R53 --> CF
    CF -->|"Dynamic: forward to ALB"| ALB
    ALB --> ING_CTRL
    ING_CTRL -->|"/* (ClusterIP)"| FE_DEP
    ING_CTRL -->|"/api/* (ClusterIP)"| BE_DEP
    BE_DEP -->|"JDBC :5432"| RDS
    BE_DEP -->|"Redis Protocol :6379"| REDIS
    BE_DEP -->|"GetSecretValue API"| SM
    BE_DEP -->|"SES API / SMTP"| SES
    SM --- KMS
    IAM_ROLES -.->|"grants access"| SM
    IAM_ROLES -.->|"grants access"| ECR
    ECR -.->|"imagePull"| FE_DEP
    ECR -.->|"imagePull"| BE_DEP
    HPA_FE -.->|"scales"| FE_DEP
    HPA_BE -.->|"scales"| BE_DEP
    GHA -.->|"kubectl apply / rollout"| EKS_CLUSTER
    GHA -.->|"docker push"| ECR
    CW_LOGS -.->|"monitors"| EKS_CLUSTER
    CW_ALARMS -.->|"alerts"| CW_METRICS

    style INTERNET fill:#f5f5f5,stroke:#9e9e9e
    style AWS fill:#fff8e1,stroke:#f57f17
    style EDGE fill:#fff3e0,stroke:#e65100
    style VPC fill:#e3f2fd,stroke:#1565c0
    style PUBLIC_SUBNETS fill:#e1f5fe,stroke:#0277bd
    style EKS_CLUSTER fill:#e8f5e9,stroke:#2e7d32
    style MTS_NS fill:#f1f8e9,stroke:#558b2f
    style DATA fill:#ede7f6,stroke:#4527a0
    style SECURITY fill:#fce4ec,stroke:#880e4f
    style REGISTRY fill:#e8eaf6,stroke:#283593
    style OBSERVABILITY fill:#e0f2f1,stroke:#00695c
    style EMAIL fill:#f3e5f5,stroke:#6a1b9a
    style CICD_LAYER fill:#fff9c4,stroke:#f9a825
```

---

## Component Inventory

| Component | Technology | Internal Port | Host Port | Database | Network |
|---|---|---|---|---|---|
| NGINX Reverse Proxy | `nginx:latest` + custom `nginx.conf` | 80 | 8081 | — | my-thai-star (bridge) |
| Traefik Reverse Proxy | `traefik:2.0` | 80 (+8080 dashboard) | 8081, 8080 | — | my-thai-star (bridge) |
| Angular SPA | Angular 12+ on NGINX | 80 | — | — | my-thai-star / my-thai-star-node |
| Java Spring Boot API | JDK 11, Spring Boot 2.4, Devon4j | 8081 | — | H2 (default) | my-thai-star (bridge) |
| Node.js NestJS API | Node.js, NestJS, TypeORM | 8081 | — | SQLite in-memory | my-thai-star-node (bridge) |
| .NET Core API | ASP.NET Core, EF Core | 8081 | 8081 | MSSQL Server | static 172.20.0.0/16 |
| MS SQL Server | `microsoft/mssql-server-linux` | 1433 | 1401 | — | static 172.20.0.0/16 |
| MailDev | `maildev/maildev` | 25 (SMTP), 80 (UI) | 1025, 8092 | — | my-thai-star-node |
| SMTP Mail Service | Custom .NET microservice | 2025 | 2025 | — | static 172.20.0.0/16 |

## Migration Recommendations

| Category | Current State | Recommended Cloud Service | Benefit | Effort |
|---|---|---|---|---|
| **Database (Java)** | H2 in-memory — data lost on pod restart | AWS RDS PostgreSQL / Azure DB for PostgreSQL | Persistence, Multi-AZ HA, automated backups | Medium |
| **Database (Node.js)** | SQLite in-memory | AWS RDS PostgreSQL / Azure DB for PostgreSQL | Persistence, horizontal scalability | Medium |
| **Database (.NET)** | MSSQL Server in Docker | Azure SQL Managed Instance / AWS RDS SQL Server | Managed patching, HA, point-in-time restore | Low |
| **Email (Java)** | Gmail SMTP / corporate SMTP | AWS SES / Azure Communication Services | Deliverability, bounce/complaint handling, templates | Low |
| **Email (Node)** | MailDev (dev-only catcher) | AWS SES / Mailgun | Production-grade delivery | Low |
| **Caching** | None currently | AWS ElastiCache Redis / Azure Cache for Redis | Session storage, API caching, ML prediction cache | Medium |
| **Object Storage** | Local filesystem (dish images) | AWS S3 / Azure Blob Storage | Scalable, CDN-backed image storage | Low |
| **Compute** | 1 replica per K8s Deployment | 2+ replicas + HPA (CPU-based) | HA, zero-downtime rolling deploys | Low |
| **Secrets** | Env vars in docker-compose | AWS Secrets Manager / Azure Key Vault | Secure credential management, rotation | Low |
| **Monitoring** | None | CloudWatch / Azure Monitor + Grafana | Observability, proactive alerting | Medium |
| **CDN** | None (SPA served by NGINX pod) | AWS CloudFront / Azure Front Door | Static asset caching, reduced latency, SSL offload | Low |
| **Container Registry** | ghcr.io (public) | AWS ECR / Azure ACR | Private registry, image scanning, lifecycle policies | Low |
| **CI/CD** | Jenkins (manual trigger) + Travis CI | GitHub Actions (`.github/` already started) | Fully automated build → test → push → deploy | Low |
| **TLS** | TLS defined in K8s Ingress spec | AWS ACM + ALB / Azure App Gateway | Managed cert renewal, HTTPS redirect enforcement | Low |
